<%- include('partials/header') %>

    <h1 id="titulo">
        post de usuario 
    </h1>

    <% for (let i = 0; i < posts.length; i++) { %>
       <article>
            <h1><%= posts[i].title %></h1>
            <p><%= posts[i].body %></p>
       </article>

    <% } %>
<%- include('partials/footer') %>
